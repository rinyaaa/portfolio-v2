title = "gitleaks config"

[extend]
useDefault = true   # gitleaks同梱のデフォルト検出ルールを継承する

# 誤検知が見つかったら、本物の秘密情報でないことを確認した上で以下を有効にする
# （空の[allowlist]テーブルはgitleaksがエラーにするため、使うときだけ追加すること）。
#
# [allowlist]
# description = "誤検知として確認済みのパターン"
# paths = [
#   '''testdata/fixtures/.*''',
# ]
# regexes = [
#   '''EXAMPLE_PLACEHOLDER_[A-Z0-9]+''',
# ]
